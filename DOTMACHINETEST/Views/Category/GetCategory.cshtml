@model IEnumerable<DOTMACHINETEST.Models.Category>

@{
    ViewData["Title"] = "GetCategory";
}

<h1>GetCategory</h1>

<p>
    <a asp-action="AddCategory"> <button type="button" class="btn btn-primary"> Add Category</button></a>

   
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryName)
            </td>
            <td>
                <td>
                    <a class="btn btn-primary" asp-action="EditCategory" asp-route-id="@item.CategoryId" role="button">Edit</a>
                    <a class="btn btn-warning" asp-action="DeleteCategoryData" asp-route-id="@item.CategoryId" role="button">Delete</a>
  @*                  <a asp-action="EditCategory" asp-route-id="@item.CategoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CategoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CategoryId">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
